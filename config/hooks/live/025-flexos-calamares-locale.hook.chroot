#!/bin/sh
set -eu

mkdir -p /etc/calamares/modules
for name in welcome.conf locale.conf keyboard.conf; do
    install -m 0644 "/usr/share/flexos/calamares/modules/$name" "/etc/calamares/modules/$name"
done
