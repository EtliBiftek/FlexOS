# Contributing

FlexOS is open source. Please use GitHub Issues for reproducible bugs and feature proposals, and pull requests for changes.

Before submitting code:

```bash
./scripts/validate.sh
```

Keep the default installation intentionally small. New default packages should have a clear reason and should not duplicate existing functionality without a strong benefit.
